name: Release

# Build the release binaries, render the documentation to PDF, and publish a
# GitHub Release whenever a version tag (vMAJOR.MINOR.PATCH) is pushed. The PDFs
# (rendered from README.adoc + docs/*.adoc by docs/build.sh) are bundled in the
# release archive and attached as individual assets. Release notes are taken from
# the matching CHANGELOG.md section.
on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  release:
    name: build and publish ${{ github.ref_name }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Install build deps (alsa feature + eframe/winit)
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libasound2-dev \
            libgl1-mesa-dev \
            libxkbcommon-dev \
            libwayland-dev \
            libxcb1-dev \
            libxcb-render0-dev \
            libxcb-shape0-dev \
            libxcb-xfixes0-dev

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - name: Check the tag matches the workspace version
        run: |
          version="${GITHUB_REF_NAME#v}"
          ./.claude/skills/release/scripts/bump-version check
          got="$(./.claude/skills/release/scripts/bump-version current)"
          if [ "$got" != "$version" ]; then
            echo "tag $GITHUB_REF_NAME does not match workspace version $got" >&2
            exit 1
          fi

      - name: Build release binaries
        run: cargo build --release --workspace --locked

      - name: Build documentation PDFs
        run: |
          sudo apt-get install -y --no-install-recommends \
            plantuml graphviz default-jre-headless
          sudo gem install --no-document asciidoctor-pdf rouge asciidoctor-diagram
          ./docs/build.sh

      - name: Package
        id: package
        run: |
          version="${GITHUB_REF_NAME#v}"
          name="rackctl-us16x08-${version}-x86_64-linux"
          mkdir -p "dist/${name}/docs"
          cp target/release/rackctl-us16x08 target/release/rackctl-us16x08-gui "dist/${name}/"
          cp README.adoc CHANGELOG.md LICENSE "dist/${name}/"
          cp build/docs/*.pdf "dist/${name}/docs/"
          tar -C dist -czf "dist/${name}.tar.gz" "${name}"
          ( cd dist && sha256sum "${name}.tar.gz" > "${name}.tar.gz.sha256" )
          echo "archive=dist/${name}.tar.gz" >> "$GITHUB_OUTPUT"
          echo "checksum=dist/${name}.tar.gz.sha256" >> "$GITHUB_OUTPUT"

      - name: Extract release notes from CHANGELOG.md
        run: |
          version="${GITHUB_REF_NAME#v}"
          awk -v ver="$version" '
            index($0, "## [" ver "]") == 1 { capture = 1; next }
            capture && index($0, "## [") == 1 { exit }
            capture { print }
          ' CHANGELOG.md > notes.md
          if [ ! -s notes.md ]; then
            echo "no CHANGELOG section for ${version}" >&2
            exit 1
          fi

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" \
            --title "$GITHUB_REF_NAME" \
            --notes-file notes.md \
            "${{ steps.package.outputs.archive }}" \
            "${{ steps.package.outputs.checksum }}" \
            build/docs/*.pdf
