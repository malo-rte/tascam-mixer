= BOSS GX-700 graphical editor — roadmap & ideas
:toc: left
:icons: font
:source-highlighter: rouge

Roadmap for the *graphical patch editor* for the BOSS GX-700 (Rackctl Stage 4),
`crates/gx700-gui` (binary `rackctl-gx700-gui`).

*Status:* the **patch librarian + level balancer** (the headline feature below) is
**implemented** -- a one-screen list of all 100 user patches with one-click
audition, an *editable patch name*, and inline output-level balancing. Each row has
*Save* (store just that patch -- name and level) and *Revert* (drop the edits back to
the on-unit values), plus a batch "Write changes to unit" that stores every pending
change; all guide the user into BULK LOAD mode. The rest of this document records the
remaining ideas and how they map onto the existing building blocks.

NOTE: BOSS and Roland are trademarks of Roland Corporation. This is an independent,
unofficial project, not affiliated with Roland.

== Headline feature: the patch librarian & level-balancer

A one-screen view of *all patches* that makes it easy to *balance their volumes* --
the most-requested idea, and a real pain point (a typical bank has output levels all
over the place: we have seen 22 %, 50 %, 80 %, 100 % in one user bank, which is
awkward live).

The view:

* A *list of every patch* (user `U001`..`U100`, and presets `P001`..`P100`), each
  row showing the patch name and its *output level* ("volume").
* *One click selects a patch and plays it* so you can hear it.
* The output level is *editable inline* (a slider / number per row) so you can nudge
  patches until their perceived loudness matches across the bank.

=== Building blocks that already exist

[cols="1,3",options="header"]
|===
| Need | Already have
| The list (name + level + chain per slot) | `Gx700::read_patch_header` -- the CLI `patches` command renders exactly this.
| Click-to-hear a patch | `preview <slot>` writes the patch into the *active sound*, non-destructively, and works in *any* mode (better than Program Change, which the GX-700 ignores in BULK LOAD mode).
| The "volume" control | The `output-level` parameter (Level/Chain offset `00`, raw `0..100`, shown as a percentage).
| Editing a level live | `set output-level …` (an individual temporary-buffer write; instant, no special mode).
| Storing an adjusted level | A whole- or part-patch memory write -- see the mode wrinkle below.
|===

=== The one wrinkle: live vs. stored (BULK LOAD)

There are two flavours of "set the volume", and they differ by device mode (see
`gx700-sysex-protocol.adoc`):

* *Live* -- changing the *active sound's* level is instant and needs no special mode.
* *Persisting* a level into a *stored patch* is a memory write, which the GX-700
  accepts only while it is in front-panel *BULK LOAD* mode.

So the natural flow is *balance live, write once*: click through the patches
adjusting each one's level by ear, then a single *"Write levels to the unit"* action
that guides the user into BULK LOAD mode and stores the adjusted patches as a batch.
That keeps the per-patch BULK LOAD dance out of the everyday loop -- you do it once,
at the end.

=== Why the balancer touches only `output-level`

A patch is a chain of gain stages, and several of them carry a "level"/"volume":

* `output-level` -- the patch's *final* master output (Level/Chain `00`).
* Preamp -- `preamp-volume`, `preamp-master`, `preamp-gain` (the dominant loudness
  driver for amp/distortion patches).
* Per-effect output levels -- `comp-level`, `wah-level`, `dist-level`, `ns-level`,
  `eq-level`, `loop-send-level`/`loop-return-level`, `speaker-mic-level`/`-direct-level`,
  `delay-effect-level`/`-direct-level` (+ tap levels), `chorus-effect-level`,
  `reverb-effect-level`/`-direct-level`.
* EQ band gains -- `eq-low-gain` / `-mid-gain` / `-high-gain` (`0..40`, 20 ≈ 0 dB).

The balancer deliberately adjusts *only* `output-level`, because it rescales the
*whole* patch uniformly -- it changes loudness *without re-voicing the sound*. Every
other level sits *inside* the signal chain, so moving it changes the tone or the
internal mix (more preamp master = more drive **and** volume, an EQ boost colours as
it lifts, etc.). That is sound-design, not balancing.

Consequence: two patches at the same `output-level` can still differ in perceived
loudness (their internal gain staging differs), but `output-level` is the correct,
tone-preserving knob to even them out. The one case it cannot cover is making a patch
*louder than its design* once `output-level` is already at 100% -- for that you would
reach into `preamp-master` / `preamp-volume`, which belongs in the *effect editor*
(below), not the balancer.

== Other candidate features (from the Stage 4 plan)

* *Effect editor* -- **implemented** as a separate *Edit* tab: a left column lists
  the patch's effect blocks in chain (signal) order, each marked with its enable
  state (●/○); selecting a block shows *only that block's* parameters on the right,
  each a live widget (checkbox / slider / combo) driven by the catalog. This split
  leaves room for *custom per-block UIs* by special-casing `show_block_params` per
  block, reusing the math shared in `rackctl-ui` (extracted from the US-16x08 GUI's
  `curves.rs`):
+
** *Equalizer* -- a live 3-band response curve (low shelf + mid peak + high shelf,
   with the output level shifting the whole curve) above a Gain/Freq/Q band table
   (`rackctl-ui::eq`).
** *Compressor* -- an enable + type selector, an *indicative* transfer curve
   (input→output dB; the device publishes no ratio/threshold-dB, so the mapping is
   approximate), and a mode-aware grid (Sustain/Attack for Compressor, Threshold/
   Release for Limiter, plus Tone/Level) -- US-16x08 style (`rackctl-ui::comp`). Reading current values goes through
  the typed model (`typed::Patch::get`) and edits stage through it (`set` →
  `to_raw`), applied live via `Gx700::set` and saved/reverted with the rest of the
  row's pending changes. Still to do here: drag-to-reorder the chain (the library
  has `RawPatch::chain` / `set_chain` and the CLI `chain` command), and a per-block
  enable toggle in the list.
* *Patch librarian* -- save / load / `backup` / `scene` from the GUI, reusing the
  US-16x08 GUI's preset-tab patterns.
* *Per-block response curves* -- reuse the US-16x08 GUI's `curves.rs` (EQ, etc.).

== Reuse

Per the Stage 4 plan, lift the *device-agnostic* parts of the US-16x08 GUI:
`poller.rs` (background reader + reconnect), `config.rs` (ProjectDirs settings +
migration), `preset_tab.rs` (generic preset rows), `curves.rs` (plotting). The new,
GX-700-specific UI is the patch list / level-balancer and the effect-chain editor.
App-id `rackctl-gx700-gui`; settings under `~/.config/rackctl/gx700/`.
